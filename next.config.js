/** @type {import('next').NextConfig} */
const nextConfig = {
  // Agar ushbu blok yoqilgan bo'lsa, muammo shundan bo'lishi mumkin
  experimental: {
    // optimizeCss: true, <-- buni o'chirib turing
  },
}

module.exports = nextConfig
